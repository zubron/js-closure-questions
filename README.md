js-closure-questions
====================

A set of questions (with tests) on closures for the JavaScript study group

## Setup
One time setup:
```
$ npm install -g grunt
$ npm install
```
To run the tests:
```
$ grunt
```
